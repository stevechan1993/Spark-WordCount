# Spark-WordCount

```
spark-submit --master local --deploy-mode client --class org.example.WordCount --name WordCount --driver-memory 1024M --driver-cores 1 --executor-memory 1G /YOUR PATH TO/spark-test-01/out/artifacts/spark_test_01_jar/spark-test-01.jar
```